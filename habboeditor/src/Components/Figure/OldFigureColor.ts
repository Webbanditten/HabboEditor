export default class OldFigureColor {
  public ColorId: string;
  public HexColor: string;

  public constructor(colourId: string, HexColor: string) {
    this.ColorId = colourId;
    this.HexColor = HexColor;
  }
}
