export default interface Config {
    figure: string
    habboimager: string
    localization: string
    figuredata: string
    postfigure: string
    postgender: string
    posturl: string
    gender: string
}